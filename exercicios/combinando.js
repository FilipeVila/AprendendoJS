//combinando Arrays

const numeros = [1,2,3,4,5];
const combinado = numeros.join('-');
console.log(combinado);

const frase = "olha bem vindo! Obrigado pela visita."
const resultado = frase.split(' ');
console.log(resultado);